1>. Create git repo and clone it in local.

2>.create code.py and add code to it . (it will save  a csv file to a new "data" folder)
3>.Do a git add , commit , push before intializing dvc.

# do py -m pip install dvc 
4>.Now we do "py -m dvc init" (creates .dvcignore, .dvc) 
5>.Now do "mkdir S3" (Creates a new S3 directory.)
6>. Now we do "dvc remote add -d myremote S3".
7>.Next "dvc add data/"  (it will throw an error , then we remove the stored data from git repository.)

Now it will ask to do :("git rm -r --cached 'data'" and "git commit -m stop tracking data" )
because inititally we were tracking data/folder from git now  we remove it for DVC to handle.

8>.Again we do "py -m dvc add data/" (creates data.dvc) then "git add.gitignore data.dvc"
  # the above mentioned are the one where is password or data which is assured by the dvc that you gave the data , i will 
  # gave you the password whenever you will made changes or ingested any new data, it will automatically recall me 
  # that there are kind of changes in the datsets.

9>. dvc commit and dvc push  (to save the versioning of data in S3,which is not available till now).
   After this commit and push we will saw 2 files pushed to S3.

10.>Do a git add commit push to mark this stage as first version of data.()
    because earlier when we saw the git status --> we will see untracked file (S3).  

11>.Now make changes to mycode.py to append  a new row in data, check changes via "py -m dvc status" 
   and when we saw the dvc status : it will gave us outputs as changed outs: modified: data.

12>. Again do dvc commit and push.--> to make tracking of the pushed data.  
    earlier it will ask to you , are you sure to proceed with the modified data , say yes or no.
    then it wil push or collect 2 more files, except the 2 which were pushed earlier in the S3 dataset. 
    And then we see (git sttaus) it will show that there is lot to update ,whatever we changed earlier
    but we'll not going to make changes to the untracked S3 bucket files.   
